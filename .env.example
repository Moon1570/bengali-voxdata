# Hugging Face token — required by stage 7 (pyannote speaker-diarization-3.1)
HF_TOKEN=

# YouTube Data API key — used by stage 2 (collect) for search/metadata.
YT_API_KEY=

# Any environment variables the user's ASR script (stage 9) needs go here too.
